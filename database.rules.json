{
  "rules": {
    "stars": {
      ".read": true,
      "$starId": {
        ".write": "newData.exists() || root.child('metadata/mergeLock').exists()",
        ".validate": "newData.hasChildren(['x', 'y', 'color', 'timestamp', 'sessionId']) && newData.child('x').isNumber() && newData.child('x').val() >= 0 && newData.child('x').val() <= 1 && newData.child('y').isNumber() && newData.child('y').val() >= 0 && newData.child('y').val() <= 1 && newData.child('color').isString() && newData.child('color').val().matches(/^#[0-9a-fA-F]{6}$/) && newData.child('message').isString() && newData.child('message').val().length <= 50 && newData.child('timestamp').isNumber() && newData.child('timestamp').val() <= now + 60000 && newData.child('sessionId').isString() && newData.child('sessionId').val().length <= 36 && (!data.exists() || newData.child('sessionId').val() === data.child('sessionId').val())"
      }
    },
    "metadata": {
      ".read": true,
      "totalStarsEver": {
        ".write": true,
        ".validate": "newData.isNumber() && newData.val() >= 0"
      },
      "mergeCount": {
        ".write": true,
        ".validate": "newData.isNumber() && newData.val() >= 0"
      },
      "mergeLock": {
        ".write": true,
        ".validate": "newData.isNumber() && newData.val() >= now - 60000 && newData.val() <= now + 60000"
      },
      "$other": {
        ".validate": false
      }
    },
    ".read": false,
    ".write": false
  }
}
