# Build output
dist/

# Dependencies
node_modules/

# Environment files
.env
.env.*
!.env.example
!.env.*.example

# Secrets / credentials
*.pem
*.key
*.p12
*.pfx
id_rsa*
id_ed25519*
serviceAccount*.json
firebase-adminsdk*.json

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Local vendored source that has been removed from the repo
react-bits-upstream/

# Large raw source media — kept locally for editing, too big for git (GitHub 100MB limit)
src/assets/_originals/

# Video files — kept out of the repo entirely
*.mov
*.mp4
*.webm
*.m4v
*.avi
public/videos/

# TypeScript incremental build cache
*.tsbuildinfo

# Deploy CLI artifacts (contain project/org identifiers; regenerated locally)
.vercel
.firebase/

# Editor/agent local settings
.claude/

# Local tooling output — not part of the site
.playwright-cli/
output/

# Local-only files: personal résumé data dump + internal dev journal (not used by the app)
profile.json
progress.md
